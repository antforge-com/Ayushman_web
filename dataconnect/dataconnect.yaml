specVersion: "v1"
serviceId: "webapp"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "webapp-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
